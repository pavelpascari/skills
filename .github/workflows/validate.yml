name: Validate plugins

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Claude Code CLI
        run: npm install -g @anthropic-ai/claude-code

      # Runs before `claude plugin validate`, which reports a version mismatch as a
      # warning and still exits 0 — CI stayed green through two releases while
      # marketplace.json and plugin.json drifted apart.
      - name: Check plugin metadata is in sync
        run: bash scripts/check-plugin-sync.sh

      # Skills point at `references/...` files loaded on demand. A renamed or
      # deleted one breaks silently: the skill still loads, the pointer leads
      # nowhere, and the guidance is simply absent.
      - name: Check reference pointers resolve
        run: bash scripts/check-references.sh

      - name: Validate marketplace
        run: claude plugin validate .

      - name: Validate each plugin
        run: |
          for dir in plugins/*/; do
            echo "Validating $dir"
            claude plugin validate "$dir"
          done
